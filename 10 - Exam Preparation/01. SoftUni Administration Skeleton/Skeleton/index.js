function solve() {
    let addBtnElement = document.querySelector('.form-control button');
    let modules = {};
    
    addBtnElement.addEventListener('click', function(e) {
        e.preventDefault();

        let lectureNameElement = document.querySelector('input[name=lecture-name]');
        let dateElement = document.querySelector('input[name=lecture-date]');
        let moduleOptionElement = document.querySelector('select[name=lecture-module]');


    })
};